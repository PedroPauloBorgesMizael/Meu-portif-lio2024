import './Card.css'

export default function Card(area, id, descricao, img) {
    return (
        <section className='container__card'>
            <h2>{area}</h2>
            <div>
                
            </div>
        </section>
    );
}